# 健身房数据分析系统

基于 Spring Boot + Vue 3 的健身房会员运动数据分析与可视化系统。

## 📋 系统要求

| 软件 | 版本要求 | 下载地址 |
|------|----------|----------|
| Java JDK | 8+ (推荐11或17) | https://adoptium.net/ |
| Maven | 3.6+ | https://maven.apache.org/download.cgi |
| Node.js | 16+ (推荐LTS) | https://nodejs.org/ |
| MySQL | 8.0+ | https://dev.mysql.com/downloads/mysql/ |

## 🚀 快速开始

### 方式一：一键启动（推荐）

双击运行 `start-all.bat`（或 `quick-start.bat`，已作为别名），脚本会自动完成：
1. 方案B数据库迁移（建库建表 + 导入真实数据 + 页面衍生数据回填）
2. 启动前后端服务
3. 打开浏览器

### 方式二：分步执行

```bash
# 1. 一键迁移 + 启动（推荐）
start-all.bat

# 2. 仅迁移不启动（可用于换机）
migrate-and-start.bat --no-start --no-pause

# 3. 大数据模式（10000用户）
migrate-and-start.bat --large

# 4. 停止系统（无阻塞）
stop.bat --no-pause
```

## 📁 项目结构

```
jianshen/
├── backend/                 # 后端 Spring Boot 项目
│   ├── gym-common/         # 公共模块
│   ├── gym-domain/         # 实体模块
│   ├── gym-repository/     # 数据访问层
│   ├── gym-service/        # 业务逻辑层
│   └── gym-web/            # Web 控制层
├── frontend/               # 前端 Vue 3 项目
│   ├── src/
│   │   ├── api/           # API 接口
│   │   ├── components/    # 公共组件
│   │   ├── layouts/       # 布局组件
│   │   ├── router/        # 路由配置
│   │   └── views/         # 页面视图
│   └── public/            # 静态资源
├── database/              # 数据库脚本
│   └── schema.sql        # 表结构
├── scripts/               # 工具脚本
├── migrate-scheme-b.bat  # 仅执行方案B迁移
├── migrate-and-start.bat # 方案B迁移并启动
├── start-all.bat         # IDEA一键入口（推荐）
├── start.bat             # 仅启动前后端
├── stop.bat              # 停止系统
└── quick-start.bat       # start-all别名
```

## 🔑 登录与注册

### 学员自助注册（真实落库）

- 前端注册页支持学员自助注册，提交后会写入 `users` 表。
- 注册成功后可直接使用该账号密码登录，返回真实 JWT。
- 注册字段：用户名、密码、真实姓名、邮箱、手机号、年龄、性别、健身目标。

### 登录方式

- 支持使用以下任一账号标识登录：
  - 用户名
  - 邮箱
  - 手机号
- 密码验证使用 BCrypt；历史明文密码在首次成功登录后会自动升级为 BCrypt。

### 角色说明

- 自助注册仅开放 `STUDENT`（学员）。
- `COACH`、`ADMIN` 账号由管理员端或初始化脚本创建。

## 🔑 测试账号（如已导入）

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 学员 | test_student | test123 |
| 教练 | test_coach | test123 |
| 管理员 | test_admin | test123 |

## 🌐 访问地址

- 前端页面: http://localhost:3000
- 后端API: http://localhost:8080

## 📱 功能特性

### 学员端
- 个人健身仪表盘（体重/体脂/BMI）
- 运动日历与记录
- 健身效果追踪（趋势图）
- 训练计划查看
- 运动库（2900+运动数据）
- 成就与排行榜
- 高峰期预警

### 教练端
- 学员总览仪表盘
- 学员管理列表
- 学员详细分析
- 训练计划管理
- 效果与对比报告

### 管理员端
- 教练管理
- 学员管理
- 健身房全局监控
- 用户行为分析

## 📊 数据说明

### 数据来源
- Kaggle健身数据集
- Fitbit运动数据
- 模拟生成数据

### 数据处理
清洗后的数据位于 `data-processing/cleaned/` 目录：
- `users.csv` - 用户数据
- `exercise_records.csv` - 运动记录
- `body_metrics.csv` - 身体指标
- `exercise_reference.csv` - 运动参考库

### 导入数据
```bash
# 导入清洗后的数据到数据库
import-data.bat
```

### 认证链路冒烟测试（注册/登录/落库）
```bash
# 一键校验：注册 -> 用户名登录 -> 邮箱登录 -> 数据库落库/密码哈希
auth-smoke-test.bat
```

## ⚙️ 数据库配置

默认配置（可在脚本中修改）：
- 主机: localhost
- 端口: 3306
- 用户名: root
- 密码: 123456
- 数据库: gym_fitness_analytics

如需修改，请编辑：
- `backend/src/main/resources/application.yml`
- 各 `.bat` 脚本中的 `DB_USER` 和 `DB_PASS`

## 🛠️ 开发说明

### 后端开发
```bash
cd backend
mvn spring-boot:run
```

### 前端开发
```bash
cd frontend
npm run dev
```

### 构建生产版本
```bash
# 后端
cd backend
mvn clean package -DskipTests

# 前端
cd frontend
npm run build
```

## 📝 常见问题

### Q: 数据库连接失败
A: 请确保 MySQL 服务已启动，且用户名密码正确

### Q: 后端启动失败
A: 检查 8080 端口是否被占用，或查看控制台错误信息

### Q: 前端页面空白
A: 检查浏览器控制台错误，确保后端服务正常运行

### Q: Maven 下载慢
A: 可配置阿里云镜像，编辑 `~/.m2/settings.xml`

### Q: npm 安装慢
A: 使用淘宝镜像：`npm config set registry https://registry.npmmirror.com`

## 📄 License

MIT License
